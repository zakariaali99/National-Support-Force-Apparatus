# 🚀 خطة التشغيل الشاملة للمنظومة — Full System Execution Plan

خطة الدليل الإرشادي الكامل خطوة بخطوة لتشغيل وإدارة **منظومة الجهاز الوطني للقوى المساندة** (منظومة التوثيق وإدارة شؤون الأفراد والعتاد) على بيئة العمل الإنتاجية (Windows Service / Waitress) أو بيئة التطوير المحلية.

---

## 🏗️ 1. المكونات والمعمارية البرمجية للمنظومة

تتكون المنظومة من:
- **خادم الخلفية (Backend API)**: مبني بنظام **Django 5.2 & Django REST Framework** ويعمل عبر خادم **Waitress WSGI** عالي الأداء.
- **الواجهة الأمامية (Frontend SPA)**: مبنية بنظام **React 18 & Vite** وهي مجمعة ومضمنة مسبقاً داخل مجلد (`frontend/dist`) لا تتطلب تثبيت Node.js على خادم الإنتاج.
- **قاعدة البيانات (Database)**: تدعم **SQLite3** افتراضياً (للتشغيل المباشر والسريع) أو **PostgreSQL** للبيئات الكبيرة.
- **إدارة الخدمة في الخلفية (Windows Service Manager)**: عبر أداة **NSSM** للإقلاع والعمل التلقائي مع الويندوز.

---

## 💻 2. طريقة التشغيل الأولى (الإنتاجية الموصى بها على الويندوز — Windows NSSM Service)

تعتمد هذه الطريقة على تشغيل المنظومة كـ **خدمة ويندوز تلقائية** تعمل في الخلفية فور إقلاع الجهاز وبدون الحاجة لفتح شاشات سوداء أو مواجهة أوامر (CMD).

### 📋 البرامج المطلوبة فقط على جهاز الويندوز:
1. **Python 3.11 أو أحدث**:
   - قم بتنزيله وتثبيته من [python.org](https://www.python.org/downloads/).
   - ⚠️ **تأكد من تفعيل خيار "Add Python to PATH" أثناء التثبيت.**
2. **أداة NSSM**:
   - تأكد من وجود ملف `nssm.exe` داخل مجلد المشروع (أو مضاف لمتغيرات النظام).

---

### 📝 الخطوات العملية خطوة بخطوة (Step-by-Step Setup):

#### الخطوة 1: فك الضغط أو فتح مجلد المشروع
قم بوضع مجلد المنظومة كاملاً على الجهاز (مثلاً: `C:\NSFA-Apparatus`).

#### الخطوة 2: تشغيل سكريبت التجهيز التلقائي (`deploy_windows.bat`)
1. اضغط مرتين (Double Click) على الملف **`deploy_windows.bat`**.
2. سيقوم الملف آلياً بـ:
   - إنشاء بيئة بايثون الافتراضية (`backend\venv`).
   - تثبيت كافة مكتبات وتطبيقات المنظومة من مجلد حزم الأوفلاين المحلية (`backend\wheels`).
   - التعرف على واجهة المستخدم المبنية مسبقاً (`frontend\dist`).
   - إنشاء وتحديث جداول قاعدة البيانات وتغذيتها بالبيانات والمصطلحات الأساسية (`migrate` & `seed_system`).

#### الخطوة 3: تثبيت وتشغيل الخدمة التلقائية (`nssm_install_services.bat`)
1. اضغط بـ **زر الماوس الأيمن** على ملف **`nssm_install_services.bat`**.
2. اختر **"Run as administrator" (تشغيل كمسؤول)**.
3. ستقوم الأداة بـ:
   - تسجيل الخدمة باسم **`NSFA-Apparatus`**.
   - ضبط التشغيل التلقائي مع إقلاع نظام الويندوز (**Automatic Startup**).
   - تشغيل الخدمة فوراً بدون الحاجة لإعادة إقلاع الجهاز.

#### الخطوة 4: فتح المنظومة من المستعرض
افتح أي متصفح إنترنت (Chrome / Edge / Firefox) وادخل على العنوان:
- **من الجهاز نفسه**: `http://localhost:8000`
- **من أجهزة الشبكة المحلية (Intranet)**: `http://[IP-Address]:8000` *(مثال: `http://192.168.1.150:8000`)*

---

## 🔑 3. بيانات الدخول الإدارية الأولى (Initial Credentials)

- **اسم المستخدم (Username)**: `admin`
- **كلمة المرور (Password)**: `admin123`

> 💡 **تلميح**: يُنصح فور الدخول للمرة الأولى بالانتقال إلى صفحة **"إدارة المستخدمين والصلاحيات"** وتغيير كلمة المرور أو إنشاء حسابات خاصة لكل مسؤول/مشغل.

---

## 🛠️ 4. كيفية إدارة الخدمة والتفتيش (Service Management)

### أوامر التحكم عبر موجه الأوامر (CMD كـ Administrator):

```cmd
:: 1. تشغيل الخدمة يدوياً
net start NSFA-Apparatus

:: 2. إيقاف الخدمة
net stop NSFA-Apparatus

:: 3. الاستعلام عن حالة الخدمة
nssm status NSFA-Apparatus

:: 4. إعادة تشغيل الخدمة
nssm restart NSFA-Apparatus
```

### متابعة سجلات الأخطاء والتشغيل (Logs Inspection):
تكتب المنظومة سجلاتها داخل المجلد:
- `logs\nssm_stdout.log` (سجل التشغيل والطلبات)
- `logs\nssm_stderr.log` (سجل الأخطاء والتحذيرات)

---

## 💻 5. طريقة التشغيل الثانية (بيئة التطوير التفاعلية — Interactive Dev Mode)

تستخدم هذه الطريقة للربط المباشر أو التعديل والتطوير على أجهزة Mac / Linux / Windows عبر التيرمنال:

### Terminal 1: خادم الخلفية (Backend Python)
```bash
cd backend
source venv/bin/activate    # على ويندوز: venv\Scripts\activate.bat
python manage.py runserver 127.0.0.1:8000
```

### Terminal 2: خادم الواجهة الأمامية (Frontend Vite Dev)
```bash
cd frontend
npm install
npm run dev
```
افتح المتصفح على: `http://localhost:5173`

---

## 📦 6. النسخ الاحتياطي واستعادة البيانات (Backups)

1. **النسخ الاحتياطي الآلي**: تعمل المنظومة على إنشاء نسخة احتياطية مشفرة يومياً بشكل تلقائي داخل مجلد `private_media/backups/`.
2. **النسخ الاحتياطي اليدوي**:
   من سطر الأوامر داخل مجلد `backend`:
   ```bash
   python manage.py backup_db
   ```
3. **تصدير وقراءة السجلات**: يمكنك الانتقال لصفحة **"إدارة النسخ الاحتياطية"** من داخل المنظومة وتحميل أي نسخة احتياطية مباشرة.

---

## ❌ 7. إزالة الخدمة من الويندوز (Uninstalling)

إذا رغبت في حظر أو إزالة الخدمة من الجهاز:
1. اضغط بـ **زر الماوس الأيمن** على **`nssm_uninstall_services.bat`**.
2. اختر **"Run as administrator"**.

---

### 🎉 النظام جاهز ومكتمل 100% للتشغيل والإنتاج المحلي!
